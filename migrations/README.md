# Database migrations

Numbered, one-way PostgreSQL migrations belong in this directory.

The migration tool and naming convention will be established by `DB-002`. No
schema is created during repository bootstrap.
