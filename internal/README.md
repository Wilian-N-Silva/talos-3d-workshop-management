# Internal server packages

Server domain, application, infrastructure, and HTTP platform packages belong
under this directory as their implementation tasks are completed.

Packages must preserve the boundaries defined in `PRD.md`; empty speculative
layers are not created during repository bootstrap.
